#!/bin/sh

# echo "dirname : [$(dirname '$(realpath $0)')]"
# echo "realpath : '$( dirname $(realpath $0) )'"
# exit
#
# # SPIRE_EXE=`which spire`
# # SPIRE_BIN_DIR=`dirname $SPIRE_EXE`
SPIRE_BIN_DIR=$( dirname $(realpath $0) )

exec "$SPIRE_BIN_DIR/use-python2.5" "$SPIRE_BIN_DIR/spire.py" "$@"
